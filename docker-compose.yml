services:
  portfolio:
    build:
      context: .
      args:
        PUBLIC_WEB3FORMS_KEY: ${PUBLIC_WEB3FORMS_KEY}
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"
